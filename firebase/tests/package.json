{
  "name": "familymap-rules-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Firestore security rules tests (run against the local emulator, demo project)",
  "type": "module",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project demo-familymap \"node --test rules.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.10.0"
  }
}
