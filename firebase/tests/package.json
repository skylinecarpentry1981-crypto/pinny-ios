{
  "name": "familymap-rules-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Firestore + Storage security rules tests (run against the local emulators, demo project)",
  "type": "module",
  "scripts": {
    "test": "firebase emulators:exec --only firestore,storage --project demo-familymap \"node --test rules.test.mjs storage.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.10.0"
  }
}
